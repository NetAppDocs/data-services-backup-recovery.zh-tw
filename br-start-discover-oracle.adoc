---
sidebar: sidebar 
permalink: br-start-discover-oracle.html 
keywords: backing up, back up, backup, backup application data, Amazon Web services, AWS, NetApp Backup and Recovery, workloads, protect, Oracle 
summary: NetApp Backup and Recovery 必須先探索工作負載，才能加以保護。 
---
= 在 NetApp Backup and Recovery 中探索 Oracle Database 工作負載
:hardbreaks:
:allow-uri-read: 
:nofooter: 
:icons: font
:linkattrs: 
:imagesdir: ./media/


[role="lead"]
在 NetApp Backup and Recovery 中探索 Oracle Database 工作負載，以便使用備份和 Snapshot 快照技術加以保護。

*必要的 NetApp Console 角色* 備份與還原超級管理員。了解 link:reference-roles.html["備份與恢復角色及權限"]。 https://docs.netapp.com/us-en/console-setup-admin/reference-iam-predefined-roles.html["了解所有服務的NetApp Console存取角色"^]。



[[configure-dns-for-air-gapped-host-discovery-in-netapp-console-local-deployment]]
== 在 NetApp Console 本機部署中設定 DNS 以實現氣隙主機探索

如果您在與網路隔離的環境中（資料庫主機無法透過標準 DNS 存取）使用 NetApp Console 本機部署，則必須暫時修改內部 RKE2 CoreDNS 組態。這可讓 Pod 將特定主機名稱解析為叢集內的 IP 位址，類似於在節點上新增 `/etc/hosts`項目。


IMPORTANT: 在進行任何變更之前，請檢查現有的 CoreDNS 組態，以避免置換任何現有的自訂設定。使用 RKE2 清單套用此變更，確保由 RKE2 營運者和執行階段一致地管理。

.開始之前
* 盡可能將 HelmChartConfig 檔案置於變更控制之下。
* 使用清晰的檔案名稱，以便明確 CoreDNS 置換的目的。
* 如果在 Corefile 中看不到預期的 DNS 記錄，請檢查 RKE2 伺服器日誌並確認清單檔案語法。


.步驟
. 檢查現有的 CoreDNS 組態，確保您沒有置換現有的變更：
+
[source, shell]
----
kubectl -n kube-system get cm rke2-coredns-rke2-coredns -o jsonpath='{.data.Corefile}'
----
. 複製以下命令區塊並編輯  `configBlock` 區域以新增您的靜態 DNS 記錄。這會在 RKE2 清單資料夾中建立一個 HelmChartConfig YAML 檔案，RKE2 會自動套用該檔案以置換 CoreDNS 組態：
+
[source, shell]
----
sudo tee /var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml >/dev/null <<'YAML'
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: rke2-coredns
  namespace: kube-system
spec:
  # NOTE: This is a HelmChartConfig that *overlays* values onto the rke2-coredns
  # HelmChart already managed by RKE2. RKE2 watches /var/lib/rancher/rke2/server/manifests
  # and will re-render the rke2-coredns Helm release whenever this file changes.
  #
  # The `servers:` list below REPLACES the chart's default server definition,
  # so every plugin that was in the stock Corefile must be present here.
  # Do NOT delete plugins you don't recognize -- they are required for cluster DNS.
  valuesContent: |-
    servers:
    - zones:
      - zone: .
      port: 53
      plugins:
      - name: errors
      - name: health
        configBlock: |-
          lameduck 10s
      - name: ready
      - name: kubernetes
        parameters: cluster.local in-addr.arpa ip6.arpa
        configBlock: |-
          pods insecure
          fallthrough in-addr.arpa ip6.arpa
          ttl 30
      # ───────────────────────── EDIT BELOW ─────────────────────────
      # Static DNS records for hosts the corporate DNS cannot resolve.
      # Format:  <IP>  <FQDN>  [optional aliases...]
      # `fallthrough` MUST stay as the last line -- it lets every other
      # name go to the upstream resolver via the `forward` plugin below.
      - name: hosts
        configBlock: |-
          1.2.3.4 example.domain.com exampleshortname
          fallthrough
      # ───────────────────────── EDIT ABOVE ─────────────────────────
      - name: prometheus
        parameters: 0.0.0.0:9153
      - name: forward
        parameters: . /etc/resolv.conf
      - name: cache
        parameters: 30
      - name: loop
      - name: reload
      - name: loadbalance
YAML
sudo chmod 600 /var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml
----
. 驗證 CoreDNS 是否已偵測到變更：
+
[source, shell]
----
kubectl -n kube-system get cm rke2-coredns-rke2-coredns -o jsonpath='{.data.Corefile}'
----
+
如果幾分鐘後變更仍未生效，請重新啟動 CoreDNS 部署，然後再次驗證 Corefile：

+
[source, shell]
----
kubectl -n kube-system rollout restart deploy/rke2-coredns-rke2-coredns
----
. 如果在探索資料庫主機後需要移除條目，請執行下列操作：
+
.. 從清單資料夾中刪除 HelmChartConfig 檔案。
.. 重新啟動 CoreDNS 部署：
+
[source, shell]
----
kubectl -n kube-system rollout restart deploy/rke2-coredns-rke2-coredns
----






[[add-an-oracle-database-host-and-discover-resources]]
== 新增 Oracle Database 主機並探索資源

新增 Oracle 資料庫主機資訊，並讓 NetApp Backup and Recovery 探索工作負載。對於每個 Console 代理程式，選擇要探索的系統。

.步驟
. 從 NetApp Console 選單中，選取 *Protection* > *Backup and Recovery*。
. 在“工作負載”下，選擇“Oracle”磁貼。
. 選擇*發現資源*。
. 在 *Workload type* 欄位中選擇 *Oracle*。
. 如果您尚未儲存此 Oracle Database 主機的憑證，請選擇「新增憑證」。
+
.. 選擇與該主機一起使用的控制台代理程式。
.. 輸入此憑證的名稱。
.. 輸入該帳戶的使用者名稱和密碼。
.. 選擇*完成*。


. *主機註冊*：新增新的 Oracle 資料庫主機。在「主機 FQDN 或 IP 位址」欄位中，輸入主機的 FQDN 或 IP 位址。對於叢集資料庫，您可以輸入叢集中任意節點的 FQDN 或 IP 位址。然後提供憑證、Console 代理程式和連接埠號碼。
. （可選）如果您為非 root 使用者選擇憑證，請執行下列操作：
+
.. 有關將選定的非 root 使用者新增至 Oracle 資料庫主機上的 sudoers 檔案的說明，請選擇 *Configure sudoers*。
.. 按照對話框中的說明操作，完成後啟用核取方塊，然後選取 *Done* 。


. *Advanced settings*：請執行以下操作：
+
.. 輸入要用於 NetApp 外掛程式的連接埠和安裝路徑。此外掛程式可實現 Oracle Database 主機與 NetApp Backup and Recovery 之間的通訊。
.. 選擇是否允許 NetApp Backup and Recovery 自動在每個主機上安裝外掛程式，或略過所有主機的自動外掛程式安裝。選取 *顯示手動安裝說明* 以取得手動安裝指引。
+
[NOTE]
====
NetApp Backup and Recovery 透過 SSH 連接到每台主機以自動安裝外掛。當出現以下任一情況時，請啟用 *Use manual installation*：

*** 一個或多個主機未執行 SSH 服務。
*** 任何主機都已安裝 NetApp 外掛程式（包括僅部分叢集成員安裝的情況）。
*** 您偏好在每個主機上手動安裝外掛程式。


====
.. 如果資料庫主機是叢集的，請啟用 *Add all hosts in the cluster* 選項以探索叢集中的所有主機。
.. 選擇是否在自動安裝外掛程式之前，在每個主機上執行預先安裝檢查。如果檢查失敗，自動外掛程式安裝就會停止。若要略過檢查並繼續安裝，請啟用 *Skip optional preinstall checks* （如果您選擇手動安裝，則會自動啟用）。


. 選擇*發現*。
+

TIP: 新增資源可能需要幾分鐘。若要查看進度，請在庫存頁面底部的狀態對話方塊中選取 *Track progress*，或從左側導覽列中選取 *Monitoring*。



.結果
Oracle 資料庫工作負載會顯示在「清單」頁面的工作負載清單中。



[[continue-to-the-netapp-backup-and-recovery-dashboard]]
== 繼續存取NetApp Backup and Recovery儀表板

. 從 NetApp Console 選單中，選取 *Protection* > *Backup and Recovery*。
. 選擇一個工作負載圖塊（例如，Oracle 資料庫）。
. 從備份和還原選單中，選擇*儀表板*。
. 審查資料保護健全狀況。面臨風險或受到保護的工作負載數量會根據新探索、受到保護和備份的工作負載而增加。

